# 라운지엑스24h 대시보드

라운지엑스24h 무인 로봇 매장의 **지점별 손익(P&L)**을 한 화면에서 보고 입력·수정할 수 있는 정적 대시보드입니다. 백엔드 없이 브라우저 `localStorage`에 데이터가 저장되며, JSON 파일로 내보내기/불러오기를 지원합니다.

## 1. 프로젝트 소개

- 매장별 매출, 투자금, 회수금, 회사 P&L을 한 화면에서 확인
- 오늘 날짜·날씨 + 지점별 운영 카드 + 기간 필터 + KPI 카드 + 매출 비중 도넛 차트 + 지점 상세 테이블
- 데이터는 "업데이트"로 바리스에서 가져오며, 화면에서 직접 수정하지 않습니다(읽기 전용)
- 데이터는 자동으로 `localStorage`에 저장되며, 화면을 닫았다가 다시 열어도 유지됩니다
- 단일 페이지 / 단일 화면 / 다크 모드 / 한국어 UI

## 2. 로컬 실행

별도 빌드 도구가 필요 없습니다. 디렉토리에서 다음 중 하나를 실행하세요.

```bash
# Python 3
python3 -m http.server 8000

# 또는 Node가 있다면
npx serve .
```

브라우저에서 <http://localhost:8000> 접속.

> `index.html`을 더블클릭해 `file://` 로 열어도 동작하지만, 일부 브라우저에서 `localStorage` 키 격리 등의 동작이 다를 수 있으니 로컬 서버 사용을 권장합니다.

## 3. 사용법

### 3.1 데이터 가져오기
- 우측 상단 **"업데이트"** 로 바리스에 로그인하면 지점·월별 매출을 가져옵니다.
- 화면의 값은 모두 읽기 전용입니다(직접 편집 불가).
- 지점 카드(오늘 매출·주문·제조·품절·운영상태)를 포함해 **모든 조회는 "업데이트"를 누를 때만** 실행됩니다.
  주기적인 자동 갱신은 없습니다 — 최신 값이 필요하면 "업데이트"를 다시 누르세요.
  앱 결제비율·OTC 평균은 조회 부하가 커서 전용 API가 준비될 때까지 빼 두었습니다.
- 가져온 데이터는 `localStorage` 에 저장되고, 1.5초 뒤 클라우드에도 자동 동기화됩니다.

### 3.2 기간 필터
- 화면 상단의 시작/종료 연월로 KPI, 차트, 표의 합계 계산 범위를 바꿀 수 있습니다.
- "전체 기간", "최근 12개월" 빠른 선택 버튼 제공.

### 3.3 데이터 초기화
- **데이터 초기화**: 모든 데이터를 지우고 빈 상태로 되돌립니다.

## 4. P&L 계산 공식

| 항목 | 공식 |
|---|---|
| 운영일자 | `오늘 - 오픈일` (일 단위) |
| 운영수익 (월) | `매출 × 운영수익률` |
| 회사 P&L (선택 기간) | `해당 기간 내 오픈 시 오픈수익(1회) + Σ 월별 운영수익` |
| 최소 회수금액 (월) | `총 투자금액 / 60` |
| 평균 매출 (월) | `Σ 매출 / 입력된 월수` |
| 평균 회수금액 (월) | `평균 매출 (월) × 20%` |
| 수익률 | `평균 회수금액 (월) / 최소 회수금액 (월)` |
| 평균 수익률 (KPI) | 모든 지점의 `수익률` 평균 |

> **참고**
> - 오픈수익은 오픈한 달이 선택 기간에 포함될 때만 1회 계산됩니다.
> - "총 회수금액"은 기간 필터와 무관하게 누적 회수금을 표시합니다(투자자 관점). KPI 카드의 합계에서도 이 값을 그대로 사용합니다.
> - 운영수익률은 지점별로 개별 설정할 수 있으며, 기본값은 20% 입니다.

## 5. GitHub Pages 배포

1. GitHub에 저장소 생성 후 푸시:
   ```bash
   git init
   git add .
   git commit -m "init: loungex pnl dashboard"
   git branch -M main
   git remote add origin https://github.com/<사용자명>/<저장소명>.git
   git push -u origin main
   ```
2. 저장소 **Settings → Pages**:
   - **Source**: `Deploy from a branch`
   - **Branch**: `main` / `(root)`
3. 1~2분 후 `https://<사용자명>.github.io/<저장소명>/` 에서 접속 가능합니다.

> 정적 파일만 있으므로 별도 빌드 단계 없이 바로 배포됩니다.

## 6. 파일 구조

```
loungex-dashboard/
├── index.html      # 마크업
├── style.css       # 다크 테마 스타일
├── script.js       # 상태/렌더/저장 로직
├── README.md
└── .gitignore
```

## 7. 데이터 스키마

```jsonc
{
  "stores": [
    {
      "id": "s_xxx",
      "name": "라운지엑스24h 강남점",
      "openDate": "2025-08-01",
      "openingProfit": 5000000,
      "operatingProfitRate": 0.2,
      "totalInvestment": 200000000
    }
  ],
  "monthly": [
    {
      "storeId": "s_xxx",
      "yearMonth": "2025-08",
      "revenue": 25000000,
      "investorPayout": 8500000
    }
  ]
}
```
